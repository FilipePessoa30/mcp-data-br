# Agente local com Ollama + mcp-ibge via stdio.
#
# mcp-ibge não precisa estar listado aqui: o script o executa via `uvx`
# (requer apenas o `uv` instalado, https://docs.astral.sh/uv/).
mcp>=1.6.0
ollama>=0.4.0
