# Agente LlamaIndex (FunctionAgent) usando as tools do mcp-ibge via MCP, com
# LLM local (Ollama). mcp-ibge é executado via `uvx` (requer `uv` instalado),
# não precisa estar listado aqui.
llama-index-core>=0.12.0
llama-index-llms-ollama>=0.5.0
llama-index-tools-mcp>=0.1.0
